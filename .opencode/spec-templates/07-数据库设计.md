# 数据库设计

## 数据库概览

### 数据库选型
- **类型**: [MySQL/PostgreSQL/MongoDB]
- **版本**: [版本号]
- **字符集**: utf8mb4
- **排序规则**: utf8mb4_unicode_ci

---

## 表结构设计

### 表1: [表名称]

**表说明**: [表用途]

| 字段名 | 类型 | 长度 | 约束 | 默认值 | 说明 |
|--------|------|------|------|--------|------|
| id | bigint | - | PK, AUTO_INCREMENT | - | 主键ID |
| created_at | datetime | - | NOT NULL | CURRENT_TIMESTAMP | 创建时间 |
| updated_at | datetime | - | NOT NULL | CURRENT_TIMESTAMP | 更新时间 |

**索引**
| 索引名 | 字段 | 类型 | 说明 |
|--------|------|------|------|
| idx_xxx | xxx | UNIQUE | 唯一索引 |

**外键**
| 字段 | 引用表 | 引用字段 | 级联操作 |
|------|--------|----------|----------|
| user_id | users | id | CASCADE |

---

### 表2: [表名称]

[同上结构]

---

## ER图
```
[使用 Mermaid 格式]

erDiagram
    USER ||--o{ ORDER : "places"
    ORDER ||--|{ ORDER_ITEM : "contains"
    USER {
        bigint id PK
        string username
        string email
        datetime created_at
    }
    ORDER {
        bigint id PK
        bigint user_id FK
        string status
        decimal total_amount
        datetime created_at
    }
```

---

## 数据字典

### 枚举值定义

**order_status (订单状态)**
| 值 | 说明 |
|----|------|
| pending | 待支付 |
| paid | 已支付 |
| shipped | 已发货 |
| completed | 已完成 |
| cancelled | 已取消 |

---

## 迁移策略
- **方式**: [Flyway/Liquibase/手动SQL]
- **版本控制**: 每次变更生成新迁移文件
- **回滚方案**: 必须提供回滚SQL

---

## 性能考虑
- 大表预估行数: [数量]
- 分区策略: [按时间/按地区/不分]
- 归档策略: [冷数据归档规则]
