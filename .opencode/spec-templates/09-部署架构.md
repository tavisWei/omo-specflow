# 部署架构（Deployment Architecture）
<!-- depends-on: 02-技术架构 -->
<!-- required-for: web, api -->
<!-- generation-order: 5 -->

<!-- AGENT: 从面试中的部署目标推导。根据规模和预算推荐方案：小项目→Vercel/Railway、中项目→Docker+Fly.io、大项目→K8s。 -->

## 部署架构总览（Deployment Overview）

本文档描述系统的完整部署架构，包括环境配置、容器化方案、CI/CD流程和运维监控体系。

---

## 部署环境配置

### 环境矩阵

| 环境 | 用途 | 域名 | 部署方式 | 自动扩缩容 |
|------|------|------|----------|------------|
| 开发环境 | 开发调试 | dev.example.com | Docker Compose | 关闭 |
| 测试环境 | QA测试 | test.example.com | Kubernetes | 开启 |
| 预发布环境 | 生产前验证 | staging.example.com | Kubernetes | 开启 |
| 生产环境 | 正式服务 | api.example.com | Kubernetes | 开启 |

### 环境配置差异

#### 开发环境配置
```yaml
# docker-compose.dev.yml
services:
  api:
    build:
      context: .
      target: development
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
      - DEBUG=true
      - LOG_LEVEL=debug
    ports:
      - "3000:3000"
```

#### 测试环境配置
```yaml
# k8s/environments/test/values.yaml
replicaCount: 2

image:
  repository: registry.example.com/api-service
  tag: test-latest

resources:
  requests:
    memory: "256Mi"
    cpu: "250m"
  limits:
    memory: "512Mi"
    cpu: "500m"

autoscaling:
  enabled: true
  minReplicas: 2
  maxReplicas: 5
```

#### 生产环境配置
```yaml
# k8s/environments/prod/values.yaml
replicaCount: 3

image:
  repository: registry.example.com/api-service
  tag: prod-stable

resources:
  requests:
    memory: "512Mi"
    cpu: "500m"
  limits:
    memory: "1Gi"
    cpu: "1000m"

autoscaling:
  enabled: true
  minReplicas: 3
  maxReplicas: 20

podDisruptionBudget:
  enabled: true
  minAvailable: 2
```

---

## 部署架构拓扑

### 生产环境架构图

```
                          ┌─────────────────────────────────────────┐
                          │              CDN / WAF                  │
                          │    (静态资源 + DDoS防护 + 安全扫描)       │
                          └─────────────────────────────────────────┘
                                                  │
                                                  ▼
                          ┌─────────────────────────────────────────┐
                          │            负载均衡 (SLB)                │
                          │    (流量分发 + SSL终结 + 健康检查)        │
                          └─────────────────────────────────────────┘
                                                  │
                                                  ▼
                          ┌─────────────────────────────────────────┐
                          │              API 网关                    │
                          │  (路由转发 + 限流限流 + 鉴权认证 + 监控)   │
                          └─────────────────────────────────────────┘
                                                  │
                          ┌───────────────────────┼───────────────────────┐
                          │                       │                       │
                          ▼                       ▼                       ▼
                  ┌───────────────┐       ┌───────────────┐       ┌───────────────┐
                  │   Ingress     │       │   Ingress     │       │   Ingress     │
                  │   Controller  │       │   Controller  │       │   Controller  │
                  │   (NodePort)  │       │   (NodePort)  │       │   (NodePort)  │
                  └───────┬───────┘       └───────┬───────┘       └───────┬───────┘
                          │                       │                       │
                          ▼                       ▼                       ▼
                  ┌───────────────┐       ┌───────────────┐       ┌───────────────┐
                  │    Pod 1      │       │    Pod 2      │       │    Pod N      │
                  │  api-service  │       │  api-service  │       │  api-service  │
                  │   (副本1)     │       │   (副本2)     │       │   (副本N)     │
                  └───────────────┘       └───────────────┘       └───────────────┘
                          │                       │                       │
                          └───────────────────────┼───────────────────────┘
                                                  │
                          ┌───────────────────────┼───────────────────────┐
                          │                       │                       │
                          ▼                       ▼                       ▼
                  ┌───────────────┐       ┌───────────────┐       ┌───────────────┐
                  │  ConfigMap    │       │    Secret     │       │     HPA       │
                  │  (配置管理)   │       │  (密钥管理)   │       │  (自动扩缩容)  │
                  └───────────────┘       └───────────────┘       └───────────────┘
                                                  │
                          ┌───────────────────────┼───────────────────────┐
                          │                       │                       │
                          ▼                       ▼                       ▼
                  ┌───────────────┐       ┌───────────────┐       ┌───────────────┐
                  │   MySQL 主    │       │    Redis      │       │   MongoDB     │
                  │  (读写分离)   │       │  (缓存/会话)   │       │   (文档存储)   │
                  │     Master    │       │    Cluster    │       │   ReplicaSet  │
                  └───────┬───────┘       └───────────────┘       └───────────────┘
                          │
                          ▼
                  ┌───────────────┐
                  │   MySQL 从    │
                  │   (只读副本)  │
                  └───────────────┘
```

### 流量治理架构

```
外部请求
    │
    ▼
┌─────────┐     ┌─────────┐     ┌─────────┐     ┌─────────┐
│   WAF   │────▶│   CDN   │────▶│   SLB   │────▶│   API   │
│  防火墙  │     │  加速   │     │  负载均衡 │     │   网关   │
└─────────┘     └─────────┘     └─────────┘     └────┬────┘
                                                      │
                          ┌───────────────────────────┤
                          │                           │
                          ▼                           ▼
                   ┌────────────┐             ┌────────────┐
                   │  金丝雀发布 │             │  蓝绿部署   │
                   │  (10%流量) │             │  (全量切换) │
                   └────────────┘             └────────────┘
```

---

## 容器化配置

### 多阶段构建 Dockerfile

#### 后端服务 (Node.js)
```dockerfile
# 构建阶段
FROM node:18-alpine AS builder
WORKDIR /app
COPY package*.json ./
RUN npm ci --only=production && \
    npm cache clean --force

# 依赖阶段
FROM node:18-alpine AS deps
WORKDIR /app
COPY package*.json ./
RUN npm ci --production && \
    npm cache clean --force

# 运行阶段
FROM node:18-alpine AS runner
WORKDIR /app

# 安全配置
RUN addgroup -g 1001 -S nodejs && \
    adduser -S nodejs -u 1001

# 从依赖阶段复制
COPY --from=deps /app/node_modules ./node_modules
COPY --from=builder /app/dist ./dist

# 复制源代码
COPY --chown=nodejs:nodejs . .

# 切换用户
USER nodejs

# 健康检查
HEALTHCHECK --interval=30s --timeout=10s --start-period=5s --retries=3 \
    CMD node -e "require('http').get('http://localhost:3000/health', (r) => process.exit(r.statusCode === 200 ? 0 : 1))"

EXPOSE 3000
ENV NODE_ENV=production
CMD ["node", "dist/server.js"]
```

#### 前端服务 (Nginx)
```dockerfile
# 构建阶段
FROM node:18-alpine AS builder
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
RUN npm run build

# 运行阶段
FROM nginx:alpine AS runner

# 复制构建产物
COPY --from=builder /app/dist /usr/share/nginx/html

# 复制 Nginx 配置
COPY nginx.conf /etc/nginx/nginx.conf

# 安全配置
RUN chown -R nginx:nginx /usr/share/nginx/html && \
    chown -R nginx:nginx /var/cache/nginx && \
    chown -R nginx:nginx /var/log/nginx && \
    chmod -R 755 /usr/share/nginx/html

EXPOSE 80 443
HEALTHCHECK --interval=30s --timeout=10s --start-period=5s --retries=3 \
    CMD wget --no-verbose --tries=1 --spider http://localhost/health || exit 1

CMD ["nginx", "-g", "daemon off;"]
```

### Docker Compose 完整配置

```yaml
# docker-compose.yml
version: '3.8'

services:
  # API 服务
  api:
    build:
      context: .
      dockerfile: Dockerfile
      target: runner
    image: registry.example.com/api-service:${VERSION:-latest}
    container_name: api-service
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=${NODE_ENV}
      - DATABASE_URL=${DATABASE_URL}
      - REDIS_URL=${REDIS_URL}
      - JWT_SECRET=${JWT_SECRET}
    volumes:
      - ./logs:/app/logs
      - /app/node_modules
    networks:
      - backend
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:3000/health', (r) => process.exit(r.statusCode === 200 ? 0 : 1))"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    deploy:
      resources:
        limits:
          cpus: '1'
          memory: 1G
        reservations:
          cpus: '0.25'
          memory: 256M

  # Nginx 反向代理
  nginx:
    image: nginx:alpine
    container_name: nginx-proxy
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - ./ssl:/etc/nginx/ssl:ro
      - ./static:/usr/share/nginx/html:ro
    depends_on:
      - api
    networks:
      - frontend
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  # 数据库服务
  postgres:
    image: postgres:15-alpine
    container_name: postgres-db
    restart: unless-stopped
    environment:
      - POSTGRES_USER=${DB_USER}
      - POSTGRES_PASSWORD=${DB_PASSWORD}
      - POSTGRES_DB=${DB_NAME}
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./backup:/backup
    ports:
      - "5432:5432"
    networks:
      - backend
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER} -d ${DB_NAME}"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Redis 缓存
  redis:
    image: redis:7-alpine
    container_name: redis-cache
    restart: unless-stopped
    command: redis-server --appendonly yes --requirepass ${REDIS_PASSWORD}
    volumes:
      - redis_data:/data
    ports:
      - "6379:6379"
    networks:
      - backend
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "${REDIS_PASSWORD}", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

networks:
  frontend:
    driver: bridge
  backend:
    driver: bridge

volumes:
  postgres_data:
  redis_data:
```

### Kubernetes 部署清单

#### Deployment 配置
```yaml
# k8s/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-service
  namespace: production
  labels:
    app: api-service
    version: v1
spec:
  replicas: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: api-service
  template:
    metadata:
      labels:
        app: api-service
        version: v1
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "3000"
        prometheus.io/path: "/metrics"
    spec:
      terminationGracePeriodSeconds: 60
      containers:
        - name: api
          image: registry.example.com/api-service:v1.0.0
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 3000
              protocol: TCP
          env:
            - name: NODE_ENV
              value: "production"
            - name: PORT
              value: "3000"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: api-secrets
                  key: database-url
            - name: REDIS_URL
              valueFrom:
                configMapKeyRef:
                  name: api-config
                  key: redis-url
          resources:
            requests:
              memory: "512Mi"
              cpu: "500m"
            limits:
              memory: "1Gi"
              cpu: "1000m"
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /ready
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
          startupProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 0
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 30
          lifecycle:
            preStop:
              exec:
                command: ["/bin/sh", "-c", "sleep 10"]
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchExpressions:
                    - key: app
                      operator: In
                      values:
                        - api-service
                topologyKey: kubernetes.io/hostname
      tolerations:
        - key: "node-type"
          operator: "Equal"
          value: "application"
          effect: "NoSchedule"
```

#### Service 配置
```yaml
# k8s/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: api-service
  namespace: production
  labels:
    app: api-service
spec:
  type: ClusterIP
  selector:
    app: api-service
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
  sessionAffinity: ClientIP
  sessionAffinityConfig:
    clientIP:
      timeoutSeconds: 10800
```

#### HPA 配置
```yaml
# k8s/hpa.yaml
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: api-service-hpa
  namespace: production
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: api-service
  minReplicas: 3
  maxReplicas: 20
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 80
  behavior:
    scaleDown:
      stabilizationWindowSeconds: 300
      policies:
        - type: Percent
          value: 10
          periodSeconds: 60
    scaleUp:
      stabilizationWindowSeconds: 0
      policies:
        - type: Percent
          value: 100
          periodSeconds: 15
        - type: Pods
          value: 4
          periodSeconds: 15
      selectPolicy: Max
```

#### Ingress 配置
```yaml
# k8s/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: api-service-ingress
  namespace: production
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
    nginx.ingress.kubernetes.io/rate-limit: "100"
    nginx.ingress.kubernetes.io/proxy-body-size: "50m"
    nginx.ingress.kubernetes.io/proxy-connect-timeout: "30"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "60"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "60"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  tls:
    - hosts:
        - api.example.com
      secretName: api-tls-secret
  rules:
    - host: api.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: api-service
                port:
                  number: 80
```

---

## CI/CD 流水线

### 流水线架构

```
代码提交
    │
    ▼
┌─────────────────────────────────────────────────────────────────┐
│                         GitLab CI                                │
│                                                                  │
│  ┌─────────┐    ┌─────────┐    ┌─────────┐    ┌─────────┐        │
│  │  Lint   │───▶│ Build   │───▶│  Test   │───▶│ Security │       │
│  │ 代码检查 │    │ 编译构建 │    │ 单元测试 │    │ 安全扫描 │       │
│  └─────────┘    └─────────┘    └─────────┘    └─────────┘       │
│                                              │                    │
│                                              ▼                    │
│                                    ┌─────────────────┐            │
│                                    │   Build Image   │            │
│                                    │   构建Docker镜像 │            │
│                                    └────────┬────────┘            │
│                                             │                      │
│                  ┌──────────────────────────┼──────────────────┐  │
│                  │                          │                   │  │
│                  ▼                          ▼                   ▼  │
│           ┌───────────┐             ┌───────────┐       ┌───────────┐
│           │   Dev     │             │   Test    │       │  Staging  │
│           │  自动部署  │             │  手动审批  │       │  手动审批  │
│           └───────────┘             └───────────┘       └───────────┘
│                                                                   │
│                                              ┌───────────────────┘
│                                              │
│                                              ▼
│                                    ┌─────────────────┐
│                                    │     Prod        │
│                                    │    蓝绿部署      │
│                                    └─────────────────┘
└─────────────────────────────────────────────────────────────────┘
```

### GitLab CI 完整配置

```yaml
# .gitlab-ci.yml
stages:
  - lint
  - build
  - test
  - security
  - image
  - deploy-dev
  - deploy-test
  - deploy-staging
  - deploy-prod

variables:
  DOCKER_DRIVER: overlay2
  DOCKER_TLS_CERTDIR: "/certs"
  IMAGE_NAME: registry.example.com/${CI_PROJECT_NAME}
  DOCKERFILE: Dockerfile

# 全局缓存配置
cache:
  key: ${CI_COMMIT_REF_SLUG}
  paths:
    - node_modules/
    - .npm/

# 代码检查阶段
code-lint:
  stage: lint
  image: node:18-alpine
  before_script:
    - npm ci --cache .npm --prefer-offline
  script:
    - npm run lint
    - npm run format:check
  cache:
    key: ${CI_COMMIT_REF_SLUG}
    paths:
      - .npm/
      - node_modules/
  only:
    - merge_requests
    - develop
    - main
  allow_failure: false

# 构建阶段
build:
  stage: build
  image: node:18-alpine
  before_script:
    - npm ci --cache .npm --prefer-offline
  script:
    - npm run build
  artifacts:
    name: "${CI_COMMIT_REF_NAME}-build"
    expire_in: 1 day
    paths:
      - dist/
      - .next/
  cache:
    key: ${CI_COMMIT_REF_SLUG}
    paths:
      - .npm/
      - node_modules/
  only:
    - merge_requests
    - develop
    - main
  allow_failure: false

# 单元测试阶段
unit-test:
  stage: test
  image: node:18-alpine
  before_script:
    - npm ci --cache .npm --prefer-offline
  script:
    - npm run test:unit -- --coverage
  coverage: '/All files[^|]*\|[^|]*\s+([\d\.]+)/'
  artifacts:
    name: "${CI_COMMIT_REF_NAME}-test-results"
    expire_in: 7 days
    reports:
      junit: junit.xml
      coverage_report:
        coverage_format: cobertura
        path: coverage/cobertura-coverage.xml
  cache:
    key: ${CI_COMMIT_REF_SLUG}
    paths:
      - .npm/
      - node_modules/
  only:
    - merge_requests
    - develop
    - main

# 集成测试阶段
integration-test:
  stage: test
  image: node:18-alpine
  services:
    - postgres:15-alpine
    - redis:7-alpine
  before_script:
    - npm ci --cache .npm --prefer-offline
    - export DATABASE_URL=postgresql://postgres:postgres@postgres:5432/test
    - export REDIS_URL=redis://redis:6379
  script:
    - npm run test:integration
  artifacts:
    name: "${CI_COMMIT_REF_NAME}-integration-results"
    expire_in: 7 days
    reports:
      junit: junit-integration.xml
  only:
    - merge_requests
    - develop

# 安全扫描 - 依赖检查
dependency-scan:
  stage: security
  image: node:18-alpine
  before_script:
    - npm ci --cache .npm --prefer-offline
  script:
    - npm audit --audit-level=high
  allow_failure: true
  only:
    - merge_requests
    - develop
    - main

# 安全扫描 - 代码扫描
security-scan:
  stage: security
  image: node:18-alpine
  before_script:
    - npm ci --cache .npm --prefer-offline
  script:
    - npm run security:check
  artifacts:
    name: "${CI_COMMIT_REF_NAME}-security"
    expire_in: 30 days
    paths:
      - security-report.json
  allow_failure: true
  only:
    - merge_requests
    - develop
    - main

# Docker 镜像构建
build-image:
  stage: image
  image: docker:24-dind
  services:
    - docker:24-dind
  before_script:
    - docker login -u ${CI_REGISTRY_USER} -p ${CI_REGISTRY_PASSWORD} ${CI_REGISTRY}
  script:
    - |
      docker build \
        --target runner \
        --cache-from ${IMAGE_NAME}:latest \
        --tag ${IMAGE_NAME}:${CI_COMMIT_SHA} \
        --tag ${IMAGE_NAME}:latest \
        --label "git.commit.sha=${CI_COMMIT_SHA}" \
        --label "git.ref=${CI_COMMIT_REF_NAME}" \
        --label "pipeline.url=${CI_PIPELINE_URL}" \
        .
    - docker push ${IMAGE_NAME}:${CI_COMMIT_SHA}
    - docker push ${IMAGE_NAME}:latest
  after_script:
    - docker logout ${CI_REGISTRY}
  only:
    - develop
    - main
  dependencies:
    - build

# 开发环境部署
deploy-dev:
  stage: deploy-dev
  image: bitnami/kubectl:latest
  before_script:
    - kubectl config use-context dev
    - kubectl secret generic api-secrets --from-literal=database-url=${DEV_DATABASE_URL} --from-literal=redis-url=${DEV_REDIS_URL} --dry-run=client -o yaml | kubectl apply -f -
  script:
    - |
      kubectl set image deployment/api-service api=${IMAGE_NAME}:${CI_COMMIT_SHA} \
        --namespace=development \
        --record=true
    - kubectl rollout status deployment/api-service --namespace=development --timeout=5m
  environment:
    name: development
    url: https://dev.example.com
    on_stop: stop-dev
  only:
    - develop
  dependencies:
    - build-image

# 停止开发环境
stop-dev:
  stage: deploy-dev
  image: bitnami/kubectl:latest
  before_script:
    - kubectl config use-context dev
  script:
    - kubectl scale deployment/api-service --namespace=development --replicas=0
  environment:
    name: development
    action: stop
  only:
    - develop
  when: manual

# 测试环境部署
deploy-test:
  stage: deploy-test
  image: bitnami/kubectl:latest
  before_script:
    - kubectl config use-context test
  script:
    - |
      kubectl set image deployment/api-service api=${IMAGE_NAME}:${CI_COMMIT_SHA} \
        --namespace=test \
        --record=true
    - kubectl rollout status deployment/api-service --namespace=test --timeout=10m
    - kubectl annotate deployment/api-service --namespace=test kubernetes.io/change-cause="${CI_COMMIT_MESSAGE}"
  environment:
    name: test
    url: https://test.example.com
  only:
    - develop
  when: manual
  dependencies:
    - build-image

# 预发布环境部署
deploy-staging:
  stage: deploy-staging
  image: bitnami/kubectl:latest
  before_script:
    - kubectl config use-context staging
  script:
    - |
      kubectl set image deployment/api-service api=${IMAGE_NAME}:${CI_COMMIT_SHA} \
        --namespace=staging \
        --record=true
    - kubectl rollout status deployment/api-service --namespace=staging --timeout=10m
    - kubectl annotate deployment/api-service --namespace=staging kubernetes.io/change-cause="${CI_COMMIT_MESSAGE}"
  environment:
    name: staging
    url: https://staging.example.com
  only:
    - main
  when: manual
  dependencies:
    - build-image

# 生产环境部署 - 蓝绿部署
deploy-prod:
  stage: deploy-prod
  image: bitnami/kubectl:latest
  before_script:
    - kubectl config use-context prod
  script:
    - |
      # 蓝绿部署：先部署到绿色版本
      kubectl set image deployment/api-service-green api=${IMAGE_NAME}:${CI_COMMIT_SHA} \
        --namespace=production \
        --record=true
      
      # 等待绿色版本就绪
      kubectl rollout status deployment/api-service-green --namespace=production --timeout=10m
      
      # 切换流量到绿色版本
      kubectl label service api-service-active --namespace=production version=blue --overwrite
      kubectl patch service api-service-active --namespace=production -p '{"spec":{"selector":{"app":"api-service-green"}}}'
      
      # 等待流量切换
      sleep 30
      
      # 验证新版本
      curl -f https://api.example.com/health || exit 1
      
      # 旧版本缩容
      kubectl scale deployment api-service-blue --namespace=production --replicas=0
  environment:
    name: production
    url: https://api.example.com
  only:
    - main
  when: manual
  dependencies:
    - deploy-staging

# 回滚任务
rollback-prod:
  stage: deploy-prod
  image: bitnami/kubectl:latest
  before_script:
    - kubectl config use-context prod
  script:
    - |
      # 快速回滚到蓝色版本
      kubectl scale deployment api-service-blue --namespace=production --replicas=3
      kubectl rollout status deployment/api-service-blue --namespace=production --timeout=5m
      kubectl patch service api-service-active --namespace=production -p '{"spec":{"selector":{"app":"api-service-blue"}}}'
  environment:
    name: production
    url: https://api.example.com
    action: rollback
  only:
    - main
  when: manual
```

### Helm Chart 部署

```yaml
# values.yaml
replicaCount: 3

image:
  repository: registry.example.com/api-service
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80
  targetPort: 3000

ingress:
  enabled: true
  className: nginx
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
  hosts:
    - host: api.example.com
      paths:
        - path: /
          pathType: Prefix
  tls:
    - secretName: api-tls
      hosts:
        - api.example.com

resources:
  limits:
    cpu: 1000m
    memory: 1Gi
  requests:
    cpu: 500m
    memory: 512Mi

autoscaling:
  enabled: true
  minReplicas: 3
  maxReplicas: 20
  targetCPUUtilizationPercentage: 70
  targetMemoryUtilizationPercentage: 80

nodeSelector:
  node-type: application

tolerations:
  - key: "node-type"
    operator: "Equal"
    value: "application"
    effect: "NoSchedule"

affinity:
  podAntiAffinity:
    preferredDuringSchedulingIgnoredDuringExecution:
      - weight: 100
        podAffinityTerm:
          labelSelector:
            matchExpressions:
              - key: app
                operator: In
                values:
                  - api-service
          topologyKey: kubernetes.io/hostname

livenessProbe:
  httpGet:
    path: /health
    port: http
  initialDelaySeconds: 30
  periodSeconds: 10

readinessProbe:
  httpGet:
    path: /ready
    port: http
  initialDelaySeconds: 5
  periodSeconds: 5

env:
  NODE_ENV: production
  PORT: "3000"
```

---

## 监控与日志

### 监控体系架构

```
┌─────────────────────────────────────────────────────────────────┐
│                         监控体系                                  │
│                                                                  │
│  ┌─────────┐    ┌─────────┐    ┌─────────┐    ┌─────────┐       │
│  │ Metrics │    │  Logs   │    │ Traces  │    │  Events │       │
│  │ 数据采集 │    │ 数据采集 │    │ 数据采集 │    │ 数据采集 │       │
│  └────┬────┘    └────┬────┘    └────┬────┘    └────┬────┘       │
│       │              │              │              │             │
│       ▼              ▼              ▼              ▼             │
│  ┌─────────┐    ┌─────────┐    ┌─────────┐    ┌─────────┐       │
│  │   PRM   │    │   ELK   │    │ SkyWalking│    │  K8s    │       │
│  │(监控存储)│    │(日志存储)│    │ (追踪存储) │    │ (事件)   │       │
│  └────┬────┘    └────┬────┘    └────┬────┘    └────┬────┘       │
│       │              │              │              │             │
│       └──────────────┴──────────────┴──────────────┘             │
│                            │                                     │
│                            ▼                                     │
│                     ┌─────────────┐                              │
│                     │  Grafana    │                              │
│                     │  可视化展示  │                              │
│                     └─────────────┘                              │
│                            │                                     │
│                            ▼                                     │
│                     ┌─────────────┐                              │
│                     │ AlertManager│                              │
│                     │   告警通知   │                              │
│                     └─────────────┘                              │
└─────────────────────────────────────────────────────────────────┘
```

### 告警规则配置

```yaml
# prometheus/alerts.yaml
groups:
  - name: api-service
    rules:
      # CPU 告警
      - alert: HighCPUUsage
        expr: rate(container_cpu_usage_seconds_total{name=~"api-service-.*"}[5m]) > 0.8
        for: 5m
        labels:
          severity: warning
          team: backend
        annotations:
          summary: "API服务CPU使用率过高"
          description: "CPU使用率超过80%，当前值: {{ $value }}"

      # 内存告警
      - alert: HighMemoryUsage
        expr: container_memory_usage_bytes{name=~"api-service-.*"} / container_spec_memory_limit_bytes{name=~"api-service-.*"} > 0.85
        for: 5m
        labels:
          severity: warning
          team: backend
        annotations:
          summary: "API服务内存使用率过高"
          description: "内存使用率超过85%，当前值: {{ $value }}"

      # 接口错误率
      - alert: HighErrorRate
        expr: rate(http_requests_total{status=~"5.."}[5m]) / rate(http_requests_total[5m]) > 0.01
        for: 2m
        labels:
          severity: critical
          team: backend
        annotations:
          summary: "API错误率过高"
          description: "5xx错误率超过1%，当前值: {{ $value }}"

      # 响应时间
      - alert: HighResponseTime
        expr: histogram_quantile(0.95, rate(http_request_duration_seconds_bucket[5m])) > 2
        for: 5m
        labels:
          severity: warning
          team: backend
        annotations:
          summary: "API响应时间过高"
          description: "P95响应时间超过2秒，当前值: {{ $value }}"

      # 服务不可用
      - alert: ServiceDown
        expr: up{job="api-service"} == 0
        for: 1m
        labels:
          severity: critical
          team: backend
        annotations:
          summary: "API服务不可用"
          description: "服务健康检查失败"

      # Pod 重启
      - alert: PodRestartingTooMuch
        expr: rate(kube_pod_container_status_restarts_total[1h]) > 0.05
        for: 5m
        labels:
          severity: warning
          team: backend
        annotations:
          summary: "Pod频繁重启"
          description: "Pod重启频率过高，当前值: {{ $value }}"
```

### 日志收集配置

```yaml
# fluentd/fluentd.conf
<source>
  @type tail
  @id input-container-log
  path /var/log/containers/*.log
  pos_file /var/log/fluentd-containers.log.pos
  tag kubernetes.*
  read_from_head true
  <parse>
    @type json
    time_format %Y-%m-%dT%H:%M:%S.%NZ
  </parse>
</source>

<filter kubernetes.**>
  @type record_transformer
  <record>
    hostname "#{Socket.gethostname}"
    service api-service
    environment production
  </record>
</filter>

<match kubernetes.**>
  @type elasticsearch
  host elasticsearch.logging.svc
  port 9200
  logstash_format true
  logstash_prefix kubernetes
  <buffer>
    @type file
    path /var/log/fluentd/buffer/elasticsearch
    flush_mode interval
    flush_interval 5s
  </buffer>
</match>
```

---

## 灾备方案

### 跨地域容灾架构

```
华东1 (杭州) - 主站点
│
├─── 应用服务 (多副本)
├─── MySQL 主库 (读写)
├─── Redis 主库
├─── MongoDB 主库
│
└──▶ 数据同步 ─▶▶ 华东2 (上海) - 备站点
                    │
                    ├─── 应用服务 (热备)
                    ├─── MySQL 从库 (只读)
                    ├─── Redis 从库
                    └─── MongoDB 从库
```

### 备份策略

| 备份类型 | 频率 | 保留时间 | 存储位置 |
|----------|------|----------|----------|
| 数据库全量 | 每日凌晨2点 | 30天 | OSS 跨区域复制 |
| 数据库增量 | 每小时 | 7天 | 本地 + OSS |
| 文件存储 | 每日凌晨3点 | 90天 | OSS 跨区域复制 |
| 配置备份 | 每次部署 | 14天 | GitLab |
| 容器镜像 | 每次构建 | 90天 | Harbor 镜像仓库 |

### 灾难恢复流程

```yaml
# 灾难恢复步骤
1. 故障检测
   - 监控系统告警
   - 人工确认故障

2. 切换决策
   - 评估故障范围
   - 决定切换到备站点

3. DNS 切换
   - 修改DNS解析
   - 切换流量到备站点

4. 验证服务
   - 健康检查
   - 功能验证

5. RTO/RPO 目标
   - RTO: 15分钟
   - RPO: 5分钟
```

---

## 环境变量配置

### 生产环境变量

```bash
# 应用配置
NODE_ENV=production
PORT=3000
LOG_LEVEL=info

# 数据库配置
DATABASE_URL=postgresql://user:pass@mysql-master:5432/dbname
DATABASE_POOL_SIZE=20
DATABASE_TIMEOUT=10000

# Redis 配置
REDIS_URL=redis://redis-cluster:6379
REDIS_PASSWORD=xxx
REDIS_MAX_RETRIES=3

# 安全配置
JWT_SECRET=xxx
JWT_EXPIRY=7d
SESSION_SECRET=xxx

# 第三方服务
AWS_S3_BUCKET=prod-assets
S3_REGION=cn-hangzhou

# 监控配置
SENTRY_DSN=https://xxx@sentry.io/xxx
PROMETHEUS_ENABLED=true
```

### 配置管理 Secret

```yaml
# k8s/secrets.yaml
apiVersion: v1
kind: Secret
metadata:
  name: api-secrets
  namespace: production
type: Opaque
stringData:
  database-url: postgresql://user:pass@mysql-master:5432/dbname
  redis-password: xxx
  jwt-secret: xxx
  sentry-dsn: https://xxx@sentry.io/xxx
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: api-config
  namespace: production
data:
  redis-url: redis://redis-cluster:6379
  log-level: info
  node-env: production
```
